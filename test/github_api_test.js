/**
 * Test case for githubApi.
 * Runs with mocha.
 */
'use strict'

const githubApi = require('../lib/helpers/github_api.js')
const assert = require('assert')
const co = require('co')

describe('github-api', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Github api', () => co(function * () {

  }))
})

/* global describe, before, after, it */
