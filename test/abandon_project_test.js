/**
 * Test case for abandonProject.
 * Runs with mocha.
 */
'use strict'

const abandonProject = require('../lib/abandon_project.js')
const assert = require('assert')
const co = require('co')

describe('abandon-project', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Abandon project', () => co(function * () {

  }))
})

/* global describe, before, after, it */
